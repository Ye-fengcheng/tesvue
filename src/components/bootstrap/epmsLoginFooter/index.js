import epmsLoginFooter from './epmsLoginFooter'

/* istanbul ignore next */
epmsLoginFooter.install = function (Vue) {
  Vue.component(epmsLoginFooter.name, epmsLoginFooter)
}

export default epmsLoginFooter
